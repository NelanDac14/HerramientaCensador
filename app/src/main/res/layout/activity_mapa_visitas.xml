<?xml version="1.0" encoding="utf-8"?>
<!--
    Layout: activity_mapa_visitas.xml
    Responsabilidad: Define la interfaz de usuario para la visualización geográfica de visitas.
    Estructura: Utiliza ConstraintLayout para posicionar controles de filtrado (Chips) sobre
    un contenedor de mapa de pantalla completa (FragmentContainerView).
-->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!--
        Grupo de Filtros Geográficos:
        Permite al usuario alternar entre visualizar las visitas del día actual o el histórico total
        directamente sobre la interfaz del mapa.
    -->
    <com.google.android.material.chip.ChipGroup
        android:id="@+id/chipGroupMapa"
        android:layout_width="wrap_content"
        android:layout_height="50dp"
        android:layout_marginTop="40dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:singleSelection="true">

        <com.google.android.material.chip.Chip
            android:id="@+id/chipHoyMapa"
            style="@style/Widget.MaterialComponents.Chip.Choice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/hoy" />

        <com.google.android.material.chip.Chip
            android:id="@+id/chipTodoMapa"
            style="@style/Widget.MaterialComponents.Chip.Choice"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/todo" />

    </com.google.android.material.chip.ChipGroup>

    <!--
        Contenedor del Mapa:
        Aloja el SupportMapFragment encargado de renderizar el motor de Google Maps.
        Ocupa el espacio restante de la pantalla debajo de los filtros.
    -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/chipGroupMapa" />

</androidx.constraintlayout.widget.ConstraintLayout>